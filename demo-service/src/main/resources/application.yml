server:
  port: 9900

spring:
  application:
    name: demo-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:127.0.0.1}:${eureka.port:8761}/eureka/

feign:
  hystrix:
    enabled: true

logging:
  path: ./log
